credentials.txt

pretend this is sensitive data. the data server itself does not check who you are.
the inbound proxy is the only thing that gates access. since the data server only
binds to 127.0.0.1, no one can reach it directly from outside the ec2 instance.
